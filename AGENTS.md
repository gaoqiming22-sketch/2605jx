# AGENTS.md（本项目给 AI 编码代理的约定）

请在开始任何任务前阅读本文件并遵循。

## 项目简介
2605班军训过程记录 → 纪念册。主人：高启明（武汉理工大学物理与力学学院，电子信息类2605班，班级临时负责人）。军训期间每天把当天情况口述给代理，代理整理进时间流；军训结束时生成“单文件 HTML 纪念册 + 1080 宽总长图 PNG”发 QQ 群一次。文字为主、照片少量点缀；武理蓝金 + 军旅元素风格。

## 关键约定（务必遵守）
1. **`js/data.js` 是唯一数据源**（`window.JX_DATA`）。更新内容 = 编辑该文件；**不要改 `js/app.js`/`styles.css` 来塞数据**。
2. 日期一律 `YYYY-MM-DD`；可先留空后补（`date` 字段）。
3. 同一天重复口述：**只更新当天那条记录，不新增重复条目**；跨天则追加新条目到 `days[]` 末尾（保持时间顺序）。
4. 记录内容面向班级纪念：训练科目/进度、当天发生的事、趣事名场面、教官语录、集体感受、少量照片。**不要写**出勤人数、伤病等管理台账（用户未要求）。
5. 隐私：只写名字等非敏感信息；不写学号、电话、身份证号。
6. 中文界面与中文内容；UTF-8 无 BOM；不联网、不加外链字体/CDN（成品要能断网打开）。
7. 照片路径相对项目根，如 `assets/photos/day-01-开营.jpg`；照片放进 `assets/photos/` 后务必确认文件真实存在。
8. 风格沿用武理蓝金体系（`#1f4e8c` 蓝 / `#c9a063` 金）+ 少量军旅橄榄绿，与 `D:\高启明-武理2026` 看板一致；校徽用 `assets/logo.png`。

## 常用命令
- 打开预览：双击 `index.html`（或让 Codex 用浏览器打开）。
- 生成成品：`node scripts/build.mjs`（需本机 Node；截图用系统 Chrome/Edge + Codex 运行时自带 playwright-core，找不到时脚本会提示并跳过 PNG）。
- 更新数据后自查：刷新/重开 `index.html` 确认中文无乱码、排版正常、无破版。

## 完成后自查清单
- 改动在浏览器刷新即生效（无缓存/路径问题）；中文无乱码。
- 有照片时 `dist\2605军训纪念册.html` 为自包含单文件（照片已 base64 内联），断网可开。
- 手机宽度（约 360px）不横向溢出、不破版。
- 生成 `总长图.png` 时确认无截断、体积 ≤10MB（超了要降采样/减少照片并说明）。
- 日期与“第N天”标注正确。
- Git 提交用清晰中文，一次提交一件事；默认分支 main，功能分支 `codex/` 前缀。
- 若对话过长，提醒用户“换个界面吧”（数据都在 data.js，新对话可继续）。