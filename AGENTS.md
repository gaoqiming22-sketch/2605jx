# AGENTS.md（本项目给 AI 编码代理的约定）

请在开始任何任务前阅读本文件并遵循。

## 项目简介
2605班军训记录网页版（五模块：总览/每日记录/语录·趣事/照片墙/安排·通知）。主人：高启明（武汉理工大学物理与力学学院，电子信息类2605班，班级临时负责人）。每天口述当天情况 → 写进 `js/data.js`；军训结束时发布到 GitHub Pages 发 QQ 群（手机/电脑都能打开）。另有桌面副本 `C:\Users\30141\Desktop\2605军训看板\` 供日常查看。

## 关键约定（务必遵守）
1. **`js/data.js` 是唯一数据源**（`window.JX_DATA`）。更新内容 = 编辑该文件；**不要改 `js/app.js`/`styles.css` 来塞数据**。
2. 日期一律 `YYYY-MM-DD`；`days[].date`、`meta.start/end` 可先留空后补。
3. 同一天重复口述：只更新当天那条，不新增重复条目；跨天则追加到 `days[]` 末尾（保持时间顺序）。
4. 内容分类：正文放 `paragraphs[]`；金句/教官语录放 `quotes[]`（支持字符串或 `{text,by}`）；趣事/名场面放 `fun[]`；通知放 `notices[]`（含日期、来源 `src`，拿不准的写“待核实”）；作息安排放 `schedule[]`。照片墙由 days.photos + gallery 自动汇总，不要重复维护。
5. 隐私：只写名字等非敏感信息；不写学号、电话、身份证号。网页发布后公开，敏感信息一律不写。
6. 中文内容、UTF-8 无 BOM；不联网、不加外链字体/CDN（成品要能断网打开）。
7. 风格沿用武理蓝金（`#1f4e8c` / `#c9a063`）+ 军旅橄榄绿；校徽 `assets/logo.png`；顶栏五页签顺序固定：总览→每日记录→语录·趣事→照片墙→安排·通知。

## 常用命令与流程
- 打开预览：双击 `index.html`（或让 Codex 用浏览器打开）。
- 生成成品：`node scripts/build.mjs`（产物在 `dist\`：单文件网页 + 总长图备份；截图用系统 Chrome/Edge + Codex 运行时自带 playwright-core）。
- **桌面副本同步**：项目文件夹是母本；每次更新完内容/样式后，把项目同步到 `C:\Users\30141\Desktop\2605军训看板\`（写桌面需用户批准权限；用 robocopy /E /XD .git 之类方式全量镜像）。若用户只改数据，可只拷 `js\data.js` 等改动文件；稳妥做法是整目录镜像。
- 发布：军训结束内容定稿后，引导用户用 GitHub 账号把网页推到 GitHub Pages 仓库（需联网+登录一次），给出网址；发布前先跑 build 与自查。

## 完成后自查清单
- 改动在浏览器刷新即生效（无缓存/路径问题）；中文无乱码。
- 五个页签都能点开、内容分类正确、空数据有空提示；手机宽度 360px 不横向溢出。
- 有照片时 `dist\2605军训纪念册.html` 为自包含单文件（断网可开）；总长图 ≤10MB。
- `index.html` 与桌面副本、与 dist 渲染一致。
- Git 提交用清晰中文，一次提交一件事；默认分支 main，功能分支 `codex/` 前缀。
- 若对话过长，提醒用户“换个界面吧”（数据都在 data.js，新对话可继续）。